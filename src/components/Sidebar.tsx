"use client";

import SidebarItems from "./SidebarItems";

const Sidebar = () => {
  return (
    <div className="mt-5 hidden md:block">
      <SidebarItems />
    </div>
  );
};

export default Sidebar;
